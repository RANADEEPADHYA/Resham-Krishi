# Place your trained model weights here.
#
# Expected filenames:
#   stage1_leaf.pth      — Stage 1 (Mulberry Leaf Disease Detection)
#   stage2_silkworm.pth  — Stage 2 (Silkworm Disease Detection)
#
# Download steps:
#   1. Open your Kaggle notebook: https://www.kaggle.com/code/sohamnimmi/silkworm-disease-detection
#   2. Go to the "Output" tab and download the model .pth files.
#   3. Rename them to stage1_leaf.pth and stage2_silkworm.pth.
#   4. Copy them into this folder.
#   5. Re-start the backend server (start.bat).
#
# If no .pth files are present, the server runs in DEMO MODE automatically.
